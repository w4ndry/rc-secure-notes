import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  inputTitle: {
    borderWidth: 1,
    borderColor: '#000',
    borderRadius: 8,
    padding: 6,
    marginBottom: 16,
  },
  inputNoteContainer: {
    flex: 1,
    borderWidth: 1,
    borderColor: '#000',
    borderRadius: 8,
  },
  inputNote: {
    padding: 6,
  },
});

export default styles;
