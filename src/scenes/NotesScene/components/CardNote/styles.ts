import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    borderWidth: 1,
    borderColor: '#ddd',
    borderRadius: 8,
    padding: 12,
    marginBottom: 16,
  },
  title: {
    color: '#000',
  },
  description: {
    color: '#000',
    fontSize: 12,
  },
});

export default styles;
